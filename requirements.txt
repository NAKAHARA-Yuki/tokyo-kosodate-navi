# ETL と CI が使う依存。本番イメージには入らない（本番は app/requirements.lock）。
#
# ハッシュ付きのロックはせず、**メジャー版の上限だけ付ける**方針（docs/adr/0007）。
# 上限が無いと、破壊的変更が入った版が誰かの環境にだけ入り、
# 「同じコードなのに手元だけ落ちる」状態になる（pandas 2→3 で実際に踏んだ）。
# 上限を上げるのは意図的な変更としてPRで行う。追従は Dependabot に任せる。
google-cloud-bigquery>=3.25.0,<4
pandas>=2.2.0,<4
requests>=2.32.0,<3
pyarrow>=16.0.0,<26
db-dtypes>=1.2.0,<2
