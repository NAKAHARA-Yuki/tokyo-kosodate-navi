# 開発・CI 用（本番イメージには入れない）
-r requirements.txt

# 本番アプリの依存は **緩いまま** 入れる。ロック（app/requirements.lock）が入るのは
# 本番イメージだけで、CI はあえて新しい版で走らせる。
# こうしておくと `make lock` で上げるときに問題になる変更を CI が先に踏んでくれる（docs/adr/0007）。
-r app/requirements.in

# 上限の付け方は requirements.txt と同じ。ここが壊れても本番データは壊れないが、
# CI が赤くなると全員の手が止まるため、勝手に上がらないようにしておく。
pytest>=9.1.1,<10
httpx>=0.28.1,<1             # FastAPI TestClient が使う
ruff>=0.16.5,<0.17           # 0.x はマイナーで規則が変わるのでマイナーで止める
pytest-playwright>=0.9.0,<1  # E2E。playwright 本体も依存として入る
# カバレッジ。**数値目標には使わない。**「一度も実行されていない経路」を
# 見つけるための道具として使う（issue #64 / docs/test-effectiveness.md）。
pytest-cov>=7.1.0,<8
# アクセシビリティの自動チェック（docs/adr/0016）。axe-core を同梱しており、
# 実行時に外部から取りに行かない。
axe-playwright-python>=0.1.8,<1
